Dao = Data, Access, Object ( 데이터에 엑세스하는 애 )

DBConnection = DB랑 연결하는 애

객체지향 해서 만들거임

static 이라 DBConnection하면 import할 수 있음

public 해야 다른 패키지에서 import해서 쓸 수 있다.

private : 해당 클래스만 사용가능
아무값도 없으면 같은 패키지만 됨
pubilc은 다른 패키지도 사용가능.

DBConnection = DB와 연결하는 책임

EmpDao = DB에 엑세스 하는 책임

책임을 전부 분할했다.

직원 한명의 정보만 받아서 보려고 한다

EmpDao에 하나 만들어보기위한 틀

이렇게 적는다.

저거의 뜻 사진.

if 로 부른 이유
 -> 한개만 부르면 되니까 while해서 다 볼 필요가 없다. ( 위에서 번호지정해줌 )

정상 출력 되는 모습.

첨엔 저렇게 짰다.

empno를 받아놓고 그값안씀 
근데 왜 스미스 나오냐? 스미스가 첫번째라서.

저 emp를 리턴하는 코드는 없어도 된다.
왜냐? try밖에 이미 emp가 존재해서 거기 들어가니까 ;

규칙들
1. EmpDao는 항상 만들어야 한다. 
 -> 데이터 요청이 필요할때 이걸 계속 호출해서 재활용 하기 위함.
 -> 모든걸 다 만들지는 말고 내 프로그램에 필요한거만 만들어 두면 됨.

데이터를 요청하면 DB에서 rs로 준다. 이걸 Dao에서 entity나 ArrayList로 변환해서 주는거임

EmpDao의 역할 : 데이터의 접근, 데이터를 자기 오브젝트로 변환


