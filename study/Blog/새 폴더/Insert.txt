insert를 배워보자

직원을 추가하려면 empno, ename, job, mgr, hiredate, sal, comm, deptno
총 8개가 필요하다.
모조리 다 적지말고 

이렇게 잡는다

Emp는 모든걸 다 들고 있기 때문에.

물론 현재상태에선 암것도 없다.

저렇게 잡고 네모박스안에 모든걸 다 적으면 되는데

너무 기니까이렇게 잡는다.

StringBuilder는 문자열을 차곡차곡 저장해준다. 어레이처럼

emp 뒤에 한칸 띄워줘야 한다. 아래의 values랑 붙어버리기 때문.

?로 적으면 값을 values안에  다 칠필요 없이 아래에서 처리할 수 있다.

뒤에 sql도 .toString을 적어야함.

int 값이 몇번째에 무슨값이 들어가냐 물어보는거

?는 1부터 시작한다. 

입사일은 sysdate로 그냥 넣었다.

중간에 값을 지정하면 한칸 건너뛰는게 아닌 그냥 연결해서 적으면 된다.

pstmt.execute();는 셀렉트 할때 씀.

pstmt.executeUpdate(); // 문장 전송 및 commit해줌.

catch를 타면 -1로 설정해야함.

테스트 해볼려고 할때

Emp에 생성자를 만들어야 한다. ( 다적기 싫으면 )

디폴트 생성자를 명시적으로 적어줘야 한다. 안그럼 오류남.

순서에 맞게 적어야함. ( 선택적 매개변수가 없어서 ..)

순서보고 다 적엇다.

db확인 

metacoding씨가 emp에 잘 들어왓다

받을때 주의해야 할점

형식에 맞게 적게 vaule값을 정해줘야 한다,.

