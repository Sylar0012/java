Board 
 - id, title, content, memberId

member
 - id, title, username, password

comment
 - id, content, boardId, memberId ( boardId, memberId는 원자성 위배땜시 여기박음)

board와 member의 관계
 - member는 board에 계시글을 쓸 수 잇다.

entity 만들고

BoardDao, MemberDao, CommentDao
BDservice

service에 게시글 목록보기 만들어라
- 개시글 전부다 뜨게 만들거
 id title username 댓글갯수 뜨게 만들어야함.

 글작성

작성번호 글 제목(필수)     닉네임 (필수)                댓 갯수 
  id        borads_title    members_username      comments_boardsId 갯수


SELECT b1.id id, b1.title title, m1.username username, 
(SELECT count(*) FROM comments WHERE boardId = b1.id) count
FROM boards b1 INNER JOIN members m1
ON b1.memberId = m1.id;