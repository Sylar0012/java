라이브러리 사용
 - 내가 직접 프로그램을 짜지 않고 다른이가 만들어 져있는 코드를 사용함
 - 자바의 기본 라이브러리 = jre 라고 함
 - 자바회사가 아닌 다른 회사의 제공 라이브러리 = thrid party 라이브러리 라 함 ( 프로그래밍을 도와주는 외부 라이브러리를 의미 )

1. 자바의 기본 라이브러리

2. 내 패키지에 java/util/Random.class 를 불러와서 쓰겠다라고 선언 
   ( 내 패키지 밖에 있는 class 는 import로 선언하고 써야함 )
3. 자동완성으로 import 잡아줌

5 1~5까지 난수 생성

-- 어떤 라이브러리를 찾으려면 구글에다 치면 다 나옴 ( 모든종류를 다 알 필요 없고 찾아보는 것도 실력 ) --
   - jre가 없으면 서드 파티도 찾아보면 됨 

